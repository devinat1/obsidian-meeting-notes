apiVersion: v1
kind: Service
metadata:
  name: pg-auth
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: pg-auth
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
      protocol: TCP
      name: postgres
