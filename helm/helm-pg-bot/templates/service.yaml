apiVersion: v1
kind: Service
metadata:
  name: pg-bot
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: pg-bot
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
      protocol: TCP
      name: postgres
