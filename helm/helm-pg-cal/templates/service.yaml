apiVersion: v1
kind: Service
metadata:
  name: pg-cal
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: pg-cal
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
      protocol: TCP
      name: postgres
