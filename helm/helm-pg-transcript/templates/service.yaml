apiVersion: v1
kind: Service
metadata:
  name: pg-transcript
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: pg-transcript
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
      protocol: TCP
      name: postgres
