apiVersion: v1
kind: Service
metadata:
  name: meeting-bot-db
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: meeting-bot-db
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
      protocol: TCP
      name: postgres
